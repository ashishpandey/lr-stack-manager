name: test

on: [push, pull_request]

jobs:
  test:
    strategy:
      fail-fast: false
      matrix:
        luaVersion: ["lua:5.4", "luajit:2.1"]
        machineTag: ["ubuntu-latest"]

    runs-on: ${{ matrix.machineTag }}

    container:
      image: nickblah/${{ matrix.luaVersion }}-luarocks

    steps:
    - uses: actions/checkout@main

    - name: add binary packages
      run: sudo apt-get install build-essential

    - name: install packages
      run: luarocks install --deps-only lr-stack-manager-dev-1.rockspec

    - name: test
      run: busted -c
